{
  "name": "maya",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/sample-assets dist/sample-assets",
    "start": "npm run build && parcel src/index.html"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.62.1"
  },
  "author": "ckZero",
  "license": "MIT"
}
